<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mercadolibre.android.pip_search_meli_app.app">

    <application
        android:name="com.mercadolibre.android.pip_search_meli_app.app.MainApplication"
        android:icon="@mipmap/pip_search_meli_app_app_ic_launcher"
        android:label="@string/pip_search_meli_app_app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Base">

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
